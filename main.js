const header = 1868;
const nav = 7268;
const main = 7267;
const div = 7263;
const footer = 7263;
const article = 7394;

const total = header + nav + main + div + footer + article;

const one = 1;
const two = 0;
if (one === two || one === one) {
  console.log("Hello, ids anônimo: " + total);
}